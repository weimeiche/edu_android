<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.navyliu.widget">

    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".unit4Lsn2Fragment.AActivity"></activity>
        <activity android:name=".unit4Lsn2Fragment.BActivity"></activity>
        <activity android:name=".unit4Lsn2Fragment.FragmentActivity" />
        <activity
            android:name=".activity.SeconedActivity"
            android:launchMode="standard" />
        <activity
            android:name=".activity.activity"
            android:launchMode="standard" />
        <activity android:name=".grid.GridActivity" />
        <activity android:name=".list.RecyclerViewActivity" />
        <activity android:name=".RatingBarActivity" />
        <activity android:name=".ProgressBarActivity" />
        <activity android:name=".SeekBarActivity" />
        <activity android:name=".SwitchActivity" />
        <activity android:name=".RadioButtonActivity" />
        <activity android:name=".ImageViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>