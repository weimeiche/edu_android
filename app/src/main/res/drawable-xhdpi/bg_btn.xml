<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
<!--    android:state_pressed 是否按下，如一个按钮触摸或者点击。-->
<!--    android:state_focused 是否取得焦点，比如用户选择了一个文本框。-->
<!--    android:state_hovered 光标是否悬停，通常与focused state相同，它是4.0的新特性-->
<!--    android:state_selected 被选中，它与focus state并不完全一样，如一个list view 被选中的时候，它里面的各个子组件可能通过方向键，被选中了。-->
<!--    android:state_checkable 组件是否能被check。如：RadioButton是可以被check的。-->
<!--    android:state_checked 被checked了，如：一个RadioButton可以被check了。-->
<!--    android:state_enabled 能够接受触摸或者点击事件-->
<!--    android:state_activated 被激活，一个布尔值，如果设置为ture，那么这个项目应该在对象被持久选择时使用（如对象的高亮状态），否则应该在对象没有被激活时使用这个状态。-->
<!--    android:state_window_focused 应用程序是否在前台，当有通知栏被拉下来或者一个对话框弹出的时候应用程序就不在前台了-->
    <item android:state_pressed="true">
        <shape>
            <gradient android:angle="90" android:centerColor="@color/theme_btn_hover" android:centerX="0.3" android:centerY="0.3" android:endColor="@color/theme_btn_end" android:gradientRadius="30dp" android:startColor="@color/theme_btn_start" android:type="radial" />
            <stroke android:width="1dp" android:color="@color/sliver" />
            <padding android:bottom="20dp" android:left="20dp" android:right="20dp" android:top="20dp" />
            <corners android:radius="60dp" />
        </shape>
    </item>
    <item android:state_pressed="false">
        <!-- 定义当button 处于pressed 状态时的形态。 -->
        <shape>
            <gradient android:angle="90" android:centerColor="@color/theme_btn_hover" android:centerX="0.3" android:centerY="0.3" android:endColor="@color/theme_btn_start" android:gradientRadius="30dp" android:startColor="@color/theme_btn_end" android:type="radial" />
            <stroke android:width="1dp" android:color="@color/sliver" />
            <padding android:bottom="20dp" android:left="20dp" android:right="20dp" android:top="20dp" />
            <corners android:radius="60dp" />
        </shape>
    </item>
    <item android:state_focused="true">
        <!-- 定义当button获得 focus时的形态 -->
        <shape>
            <gradient android:angle="90" android:endColor="@color/theme_btn_hover" android:startColor="@color/theme_btn_hover" android:type="linear" />
            <stroke android:width="1dp" android:color="@color/sliver" />
            <padding android:bottom="20dp" android:left="20dp" android:right="20dp" android:top="20dp" />
            <corners android:radius="60dp" />
        </shape>
    </item>

</selector>